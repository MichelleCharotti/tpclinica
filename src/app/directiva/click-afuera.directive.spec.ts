import { ClickAfueraDirective } from './click-afuera.directive';

describe('ClickAfueraDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickAfueraDirective();
    expect(directive).toBeTruthy();
  });
});
