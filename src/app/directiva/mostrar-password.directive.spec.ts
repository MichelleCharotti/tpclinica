import { MostrarPasswordDirective } from './mostrar-password.directive';

describe('MostrarPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new MostrarPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
